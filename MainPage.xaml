<Page
    x:Class="LearnOneNote.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LearnOneNote"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="Items" Grid.Column="0" ItemsSource="{Binding ItemList}" SelectionChanged="newSelect" Grid.RowSpan="2" Padding="5" />

        <Border Grid.Column="1" Grid.Row="0" BorderThickness="0,0,0,2" BorderBrush="Brown" Margin="10,0,10,0"/>

    </Grid>
</Page>
